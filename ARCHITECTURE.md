# System Architecture - Library Management System with MongoDB

## 📐 Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     USER INTERFACE LAYER                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  Main Menu   │  │  Admin Menu  │  │  User Menu   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         LibraryManagementSystem (Main Class)             │  │
│  │  • User Management    • Book Management                  │  │
│  │  • Borrow/Return      • Fine Calculation                 │  │
│  │  • Search/Browse      • Category Management              │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   DESIGN PATTERNS LAYER                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Factory    │  │  Decorator   │  │  Observer    │         │
│  │   Pattern    │  │   Pattern    │  │   Pattern    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│  ┌──────────────┐                                              │
│  │  Singleton   │                                              │
│  │   Pattern    │                                              │
│  └──────────────┘                                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                  DATA ACCESS LAYER (DAL)                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              MongoDBManager (Singleton)                  │  │
│  │  • Connection Management  • CRUD Operations              │  │
│  │  • Transaction Logging    • Error Handling               │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE LAYER                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                MongoDB (localhost:27017)                 │  │
│  │  Database: library_management_db                         │  │
│  │  ┌────────┐  ┌────────┐  ┌──────────────┐  ┌──────────┐│  │
│  │  │ users  │  │ books  │  │ transactions │  │categories││  │
│  │  └────────┘  └────────┘  └──────────────┘  └──────────┘│  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### Borrow Book Flow:
```
User Input
    │
    ▼
┌─────────────────┐
│ User Menu       │
│ (Option 4)      │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Validate User   │
│ Permissions     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Check Book      │
│ Availability    │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Update Memory   │
│ (In-Memory)     │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Save to MongoDB │
│ • User Data     │
│ • Book Copies   │
│ • Transaction   │
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ Notify Observer │
│ (Event Manager) │
└─────────────────┘
    │
    ▼
Display Confirmation
```

---

## 🏗️ Class Structure

```
LibraryManagementSystem
├── Design Patterns
│   ├── Factory Pattern
│   │   ├── UserFactory
│   │   │   ├── AuthorizedUser
│   │   │   └── UnauthorizedUser
│   │   └── BookFactory
│   │       ├── RegularBook
│   │       └── ReferenceBook
│   │
│   ├── Decorator Pattern
│   │   ├── UserFeaturesDecorator
│   │   │   ├── BasicUserFeatures
│   │   │   └── PremiumFeaturesDecorator
│   │   └── BookDecorator
│   │       ├── BasicBookDisplay
│   │       ├── AvailableStatusDecorator
│   │       └── CategoryDecorator
│   │
│   ├── Observer Pattern
│   │   ├── LibraryEventManager (Singleton)
│   │   └── Observers
│   │       ├── EmailNotificationObserver
│   │       ├── ConsoleNotificationObserver
│   │       └── FineNotificationObserver
│   │
│   └── Singleton Pattern
│       ├── LibraryEventManager
│       └── MongoDBManager
│
├── Core Classes
│   └── User
│       ├── username
│       ├── password
│       ├── userType
│       ├── favourites[]
│       ├── borrowedBooks[]
│       └── totalFine
│
└── Database Layer
    └── MongoDBManager
        ├── User Operations
        │   ├── saveUser()
        │   ├── loadUser()
        │   ├── loadAllUsers()
        │   └── deleteUser()
        │
        ├── Book Operations
        │   ├── saveBook()
        │   ├── loadAllBooks()
        │   ├── updateBookCopies()
        │   └── deleteBook()
        │
        ├── Category Operations
        │   ├── saveCategories()
        │   ├── loadCategories()
        │   └── loadCategoryBooks()
        │
        └── Transaction Operations
            ├── saveTransaction()
            └── getUserTransactions()
```

---

## 🔌 Component Interaction

```
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│              │  uses   │              │  uses   │              │
│  Main Class  │────────▶│   Patterns   │────────▶│  DB Manager  │
│              │         │              │         │              │
└──────────────┘         └──────────────┘         └──────────────┘
       │                        │                        │
       │                        │                        │
       ▼                        ▼                        ▼
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│  User Input  │         │  Business    │         │   MongoDB    │
│  Validation  │         │  Logic       │         │   Database   │
└──────────────┘         └──────────────┘         └──────────────┘
```

---

## 📦 Dependency Structure

```
LibraryManagementSystem.java
    │
    ├── Uses: MongoDBManager.java
    │   └── Depends on:
    │       ├── mongodb-driver-sync (4.11.1)
    │       ├── bson (4.11.1)
    │       └── slf4j-api (2.0.9)
    │
    ├── Uses: Design Pattern Classes
    │   ├── UserFactory
    │   ├── BookFactory
    │   ├── Decorators
    │   └── Observers
    │
    └── Reads: config.properties
        └── MongoDB connection settings
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│         User Authentication             │
│  ┌───────────────────────────────────┐  │
│  │  Username/Password Validation     │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│      Authorization Layer                │
│  ┌───────────────────────────────────┐  │
│  │  User Type Check                  │  │
│  │  • Admin: Full Access             │  │
│  │  • Authorized: Limited Access     │  │
│  │  • Unauthorized: Read Only        │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│       Database Access                   │
│  ┌───────────────────────────────────┐  │
│  │  MongoDB Connection               │  │
│  │  • Local: localhost:27017         │  │
│  │  • No Auth (Development)          │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 📊 Database Schema Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    library_management_db                    │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐      ┌──────────────┐     ┌──────────────┐
│    users     │      │    books     │     │ transactions │
├──────────────┤      ├──────────────┤     ├──────────────┤
│ username     │      │ title        │     │ username     │
│ password     │      │ copies       │     │ bookTitle    │
│ userType     │      │ bookType     │     │ action       │
│ favourites[] │      │ category     │     │ date         │
│ borrowed[]   │      │ createdAt    │     │ timestamp    │
│ totalFine    │      │ lastModified │     └──────────────┘
└──────────────┘      └──────────────┘             │
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                              ▼
                      ┌──────────────┐
                      │  categories  │
                      ├──────────────┤
                      │ name         │
                      │ books[]      │
                      │ lastModified │
                      └──────────────┘
```

---

## 🔄 State Diagram - User Session

```
┌─────────┐
│  Start  │
└────┬────┘
     │
     ▼
┌─────────────┐
│ Not Logged  │
│     In      │
└─────┬───────┘
      │
      │ Login
      ▼
┌─────────────┐
│  Logged In  │
│   (User)    │
└─────┬───────┘
      │
      ├─── Borrow Book ───▶ Update DB ───┐
      │                                   │
      ├─── Return Book ───▶ Update DB ───┤
      │                                   │
      ├─── Add Favorite ──▶ Update DB ───┤
      │                                   │
      └─── Logout ────────────────────────┘
                                          │
                                          ▼
                                    ┌─────────┐
                                    │  Save   │
                                    │  to DB  │
                                    └─────────┘
```

---

## 🎯 Design Pattern Implementation

### 1. Factory Pattern
```
UserFactory.createUserType(type)
    │
    ├─── "authorized" ──▶ new AuthorizedUser()
    └─── "unauthorized" ─▶ new UnauthorizedUser()

BookFactory.createBookType(type)
    │
    ├─── "regular" ──▶ new RegularBook()
    └─── "reference" ─▶ new ReferenceBook()
```

### 2. Decorator Pattern
```
BasicUserFeatures
    │
    └─── + PremiumFeaturesDecorator
         └─── Enhanced Features

BasicBookDisplay
    │
    ├─── + AvailableStatusDecorator
    │    └─── Shows availability
    │
    └─── + CategoryDecorator
         └─── Shows category
```

### 3. Observer Pattern
```
LibraryEventManager
    │
    ├─── EmailNotificationObserver
    ├─── ConsoleNotificationObserver
    └─── FineNotificationObserver
         │
         └─── notifyObservers(event, message)
              └─── All observers updated
```

### 4. Singleton Pattern
```
MongoDBManager.getInstance()
    │
    └─── Single instance for entire application
         └─── Manages one database connection
```

---

## 📈 Performance Considerations

```
┌─────────────────────────────────────────┐
│         In-Memory Cache                 │
│  • Fast read operations                 │
│  • Immediate updates                    │
│  • No network latency                   │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│      Asynchronous Saves                 │
│  • Non-blocking operations              │
│  • Background persistence               │
│  • User experience maintained           │
└─────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│         MongoDB Storage                 │
│  • Indexed collections                  │
│  • Efficient queries                    │
│  • Scalable storage                     │
└─────────────────────────────────────────┘
```

---

## 🔧 Configuration Flow

```
config.properties
    │
    ├─── mongodb.connection.string
    ├─── mongodb.database.name
    ├─── mongodb.collection.users
    ├─── mongodb.collection.books
    ├─── mongodb.collection.transactions
    └─── mongodb.collection.categories
         │
         ▼
    MongoDBManager
         │
         └─── Establishes connection
              └─── Creates/uses collections
```

---

This architecture provides:
- ✅ **Separation of Concerns**: Clear layer separation
- ✅ **Scalability**: Easy to add new features
- ✅ **Maintainability**: Well-organized code structure
- ✅ **Testability**: Each component can be tested independently
- ✅ **Flexibility**: Design patterns allow easy modifications
- ✅ **Reliability**: Robust error handling and data persistence
